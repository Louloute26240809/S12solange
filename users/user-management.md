whoami *show current user

useradd * create user
    sudo useradd newuser

passwd *set password
    sudo passwd newuser

apt update *update package list
    sudo apt update
apt install *install software
    sudo apt install nginux
