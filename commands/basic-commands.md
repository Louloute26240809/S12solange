pwd *print Working directory
/home/solange

ls *list files and folders
    ls -al *detailed list

cd *change directory / move into a folder
cd .. go back
cd ~ go home

mkdir *create a folder

touch *create a file

mv *move or rename a file

rm * delete a file
rm -r *delete empty folder
rm -rf *delete a folder with his content

cat *show file content
head *show first lines
tail *show last lines

man *show manual
    man ls
--help *quick help
    ls --help
ping *test internet connection
    ping google.com
curl *get dat from a website
    curl ifconfig.me

